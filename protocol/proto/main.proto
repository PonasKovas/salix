syntax = "proto3";
package main;

service Main {
	// must always be the first packet sent
	rpc VersionCheck (Version) returns (Version);

	rpc SayHello (Empty) returns (Empty);
}

message Version {
	uint32 major = 1;
	uint32 minor = 2;
	uint32 patch = 3;
}

message Empty {}
